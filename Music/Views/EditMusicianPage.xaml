<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Music.Views.EditMusicianPage"
    Title="Редактировать музыканта">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="12">

            <Label Text="Имя:" TextColor="White" />
            <Entry x:Name="NameEntry" Placeholder="Введите имя" />

            <Label Text="Жанр:" TextColor="White"  />
            <Picker x:Name="GenrePicker" Title="Выберите жанр" >
                <Picker.ItemsSource>
                    <x:Array Type="{x:Type x:String}">
                        <x:String>Rock</x:String>
                        <x:String>Pop</x:String>
                        <x:String>Jazz</x:String>
                        <x:String>Electronic</x:String>
                        <x:String>Hip-Hop</x:String>
                        <x:String>Other</x:String>
                    </x:Array>
                </Picker.ItemsSource>
            </Picker>

            <Label Text="Страна:" TextColor="White" />
            <Entry x:Name="CountryEntry" Placeholder="Введите страну"  />

            <Label Text="Год дебюта:" TextColor="White"  />
            <HorizontalStackLayout Spacing="10">
                <Stepper x:Name="DebutStepper" Minimum="1900" Maximum="2100" Increment="1" />
                <Label x:Name="DebutLabel" TextColor="White"  />
            </HorizontalStackLayout>

            <HorizontalStackLayout Spacing="10">
                <Label Text="Активен"  VerticalOptions="Center" TextColor="White" />
                <Switch x:Name="IsActiveSwitch" />
            </HorizontalStackLayout>

            <HorizontalStackLayout Spacing="10">
                <Button Text="💾 Сохранить" Clicked="OnSaveClicked" />
                <Button Text="Отмена" Clicked="OnCancelClicked" />
            </HorizontalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
